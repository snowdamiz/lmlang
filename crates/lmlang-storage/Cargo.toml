[package]
name = "lmlang-storage"
version = "0.1.0"
edition = "2021"

[dependencies]
lmlang-core = { path = "../lmlang-core" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
thiserror = "2"
